{
  "name": "course-copilot",
  "private": true,
  "description": "Scripts only; app code lives in frontend/ and api/",
  "scripts": {
    "db:apply": "node scripts/apply-supabase-sql.mjs"
  },
  "devDependencies": {
    "postgres": "^3.4.5"
  }
}
