{
  "name": "course-copilot",
  "private": true,
  "description": "Root scripts and tooling; application code lives in frontend/ and api/",
  "scripts": {
    "db:apply": "node scripts/apply-supabase-sql.mjs"
  },
  "devDependencies": {
    "postgres": "^3.4.5"
  }
}
